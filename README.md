# Cloud Security Learning Notes - Day 1

## What I did today

Today I started learning Linux and Cloud Security fundamentals using Bandit (OverTheWire).

---

## Topics I practiced

### 1. Linux basics
- mkdir, cd, ls
- file handling with cat
- working with hidden files

---

### 2. SSH connections
Learned how to connect to remote servers:

```bash
ssh user@host -p 2220
grep "text" file
sort file | uniq -u
find . -name "*.log"
nc localhost 30000
nc -l 12345
openssl s_client -connect localhost:30001
base64 -d file.txt
tr 'A-Za-z' 'N-ZA-Mn-za-m'
* * * * * command
git clone <repo>
git log -p
git branch -a
git tag
